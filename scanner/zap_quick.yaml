env:
  contexts:
    - name: target
      urls: ["${TARGET_URL}"]
jobs:
  - type: spider
    parameters: {context: target, maxDuration: 5}
  - type: passiveScan-wait
  - type: report
    parameters: {template: traditional-json, reportDir: /zap/wrk, reportFile: zap-report.json}

